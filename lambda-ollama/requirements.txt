# NusantaraAI Lambda Layer - Python 3.11
# Dipakai untuk build Lambda Layer (bukan di-install langsung ke Lambda)
#
# Cara install:
#   pip install -r requirements.txt -t python/lib/python3.11/site-packages/

scikit-learn==1.4.2
scikit-surprise==1.1.3
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# boto3 sudah tersedia di runtime Lambda — tidak perlu dimasukkan layer
