# NusantaraAI Lambda Layer - Python 3.10 / 3.11
#
# scikit-surprise TIDAK ada di sini karena:
#   - Tidak punya pre-built wheel → gagal di-build di SageMaker
#   - Tidak dibutuhkan saat inference (model pakai UserAverage, bukan SVD)
#   - lambda_function.py sudah punya stub agar pickle.load tidak crash
#
# boto3 sudah tersedia di Lambda runtime — tidak perlu dimasukkan layer

scikit-learn==1.4.2
pandas>=2.2.2
numpy==1.26.4
pyarrow==16.1.0